/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable static export to enable dynamic rendering
  // Remove 'output: export' to enable server-side rendering
}

module.exports = nextConfig